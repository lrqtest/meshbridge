# macOS: launchd plist 例 (~/Library/LaunchAgents/io.meshbridge.agent.plist), token 0600, allowed_roots 例 /Users/<u>/Projects.
# tailscale 用官方 pkg, >=1.86; GUI 登录 --login-server https://hs.example.com 后再跑 agent.
